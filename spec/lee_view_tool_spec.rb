require "spec_helper"

describe LeeViewTool do
  it "has a version number" do
    expect(LeeViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
